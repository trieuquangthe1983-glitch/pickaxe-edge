# Copy this file to `.streamlit/secrets.toml` and fill in real values.
# `.streamlit/secrets.toml` is gitignored — never commit real keys.
#
# On Streamlit Community Cloud, paste this content (with real values) into
# App settings -> Secrets in the dashboard. Do NOT push secrets.toml to the repo.

YOUTUBE_API_KEY = "AIzaSy...your-key-here"
